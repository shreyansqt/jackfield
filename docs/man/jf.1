.TH JF 1 "2026-08-25" "jf" "jf is the patchbay for agent credentials: workspace-scoped CLI identities, plus the hub"
.SH NAME
jf - jf is the patchbay for agent credentials: workspace-scoped CLI identities, plus the hub
.SH SYNOPSIS
\fBjf\fP [\fIoptions\&.\&.\&.\fP] [\fIargument\&.\&.\&.\fP]
.SH DESCRIPTION
jf answers two questions\&. Which identity does a command line tool get in this directory? And where does that credential come from?
.PP
.PP
A manifest, jackfield\&.yaml, answers the first question\&. It maps a directory to a workspace, and a workspace to the profiles that each command may use\&. "jf run" starts a tool under one of those profiles\&. It removes the ambient credential variables, and it rejects the arguments that would select another account, so the child process cannot change identity\&.
.PP
.PP
The jackfield hub answers the second question\&. The hub holds the credentials themselves, and this machine is a cache\&. "jf login" signs this machine in to the hub\&. "jf cred get" reads one credential, and "jf cred set" writes one\&.
.PP
.PP
A workspace command needs a manifest and no hub\&. A hub command needs a hub address and, except for "jf login", a device token\&. It needs no manifest, so a fresh machine runs "jf login" before any jackfield\&.yaml exists there\&.
.SH OPTIONS
.TP
\fB--config\fP
Read this manifest instead of searching for jackfield\&.yaml
.SH COMMANDS
.TP
\fBcred\fP
.RS 4
Read one credential from the hub, or write one
.RE
.TP
\fBCOMMANDS\fP
.RS 4
\fBget\fP NAME
.RS 4
Print one credential to standard output, for scripts
.RE
.TP
\fBOPTIONS\fP
.RS 4
\fB--no-cache\fP
Ask the hub even when a fresh cached copy exists
.RE
.TP
\fBinstall\fP NAME
.RS 4
Make one hub credential usable by its local tool
.RE
.TP
\fBset\fP NAME
.RS 4
Store a credential in the hub
.RE
.TP
\fBOPTIONS\fP
.RS 4
\fB--identity\fP
Who this credential acts as, for the status panel
.TP
\fB--stdin\fP
Read the secret from standard input instead of prompting
.TP
\fB--ticket\fP
An approval ticket from the hub's approval page
.RE
.RE
.TP
\fBdevice\fP
.RS 4
List the machines that hold a device token, or revoke one
.RE
.TP
\fBCOMMANDS\fP
.RS 4
\fBlist\fP
.RS 4
List every machine that is signed in to the hub
.RE
.TP
\fBrevoke\fP NAME
.RS 4
Remove one machine's device token
.RE
.RE
.TP
\fBlogin\fP
.RS 4
Sign this machine in to the hub
.RE
.TP
\fBOPTIONS\fP
.RS 4
\fB--browser\fP
Open a browser even when this machine looks headless
.TP
\fB--device-code\fP
Print the code and URL for another device instead of opening a browser
.TP
\fB--name\fP
The name this machine gets in `jf device list` (default: the short hostname)
.RE
.TP
\fBlogout\fP
.RS 4
Sign this machine out of the hub
.RE
.TP
\fBman\fP
.RS 4
Print the manual page in roff format
.RE
.TP
\fBOPTIONS\fP
.RS 4
\fB--config\fP
Read this manifest instead of searching for jackfield\&.yaml
.TP
\fB--h --help\fP
help for man
.RE
.TP
\fBresolve\fP COMMAND [ARGS\&.\&.\&.]
.RS 4
Show which identity a command would get, and run nothing
.RE
.TP
\fBOPTIONS\fP
.RS 4
\fB--profile\fP
Select one of the profiles that this workspace allows
.RE
.TP
\fBrun\fP COMMAND [ARGS\&.\&.\&.]
.RS 4
Run a command line tool under the identity of this directory
.RE
.TP
\fBOPTIONS\fP
.RS 4
\fB--profile\fP
Select one of the profiles that this workspace allows
.RE
.TP
\fBschema\fP
.RS 4
Print the whole command tree as JSON, for an agent
.RE
.TP
\fBOPTIONS\fP
.RS 4
\fB--json\fP
Print the command tree as JSON
.RE
.TP
\fBstatus\fP
.RS 4
Show where every connection stands
.RE
.TP
\fBversion\fP
.RS 4
Print the version of jf
.RE
